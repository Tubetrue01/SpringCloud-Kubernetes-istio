spring:
  application:
    name: test-provider
  cloud:
    kubernetes:
      config:
        name: test-provider
        namespace: zpf
        enabled: true
      secrets:
        paths: /etc/secrets/test-provider
